import React from react; // Import React from react.
function MoviesList(movies) { // Inside the javascript expression, map the movies array
const MovieArr = this.state.movies.map((movies, i) =>  // Create a const arrow function called MoviesList. // The function inside map should take in two arguments, movies and i (for index).
    <li key={number.toString({i})}>{movies}</li>
); // The list item should have an attribute called key, that takes in i as a javascript expression.

return ( // Return an unordered list
    <ul>{Movies}</ul>
); // Inside the unordered list, embed a javascript expression (curly brackets).
// The function should return a list item, with a javascript expression movies (the same variable as map).
}
export default class MoviesList{};
// Outside the arrow function, export the function MoviesList as a default.
